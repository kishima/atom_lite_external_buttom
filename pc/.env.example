# docker-compose 用の設定（任意）。cp .env.example .env で使う。

# ATOM Lite のシリアルポート（`ls /dev/ttyACM* /dev/ttyUSB*` で確認）
SERIAL_PORT=/dev/ttyACM0

# ボーレート（ファーム側は 115200 固定）
SERIAL_BAUD=115200

# LED の色（RRGGBB）。既定は 正常=緑 / 押下=赤。
NORMAL_COLOR=00FF00
PRESSED_COLOR=FF0000
