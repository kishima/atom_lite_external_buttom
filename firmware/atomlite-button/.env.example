# Rakefile が読み込むビルド設定の上書き（任意）。cp .env.example .env で使う。

# 既存の ESP-IDF イメージを使いたい場合（この場合 rake image は不要）
# DOCKER_IMAGE=espressif/idf:v5.5

# シリアルポートを固定したい場合（未指定なら rake check-port で自動検出）
# SERIAL_PORT=/dev/ttyACM0

# フラッシュ書き込みのボーレート
# FLASH_BAUD=115200
